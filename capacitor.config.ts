import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'av_formadora_comp01',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
